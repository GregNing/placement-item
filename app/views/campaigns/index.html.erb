<p style="color: green"><%= notice %></p>

<div class="col-md-12">

  <%= render "form", campaign: Campaign.new %>

  <%= search_form_for @q, data: { controller: 'search-form', turbo_frame: 'campaigns', turbo_action: 'advance'} do |f| %>
  <div class="input-group mb-3">
    <%= f.search_field :name_cont, class: 'form-control', placeholder: 'Search Campaign Name...', data: {action: "input->search-form#search"} %>
  </div>
  <% end %>
  <%= render "campaigns", campaigns: @campaigns %>
</div>